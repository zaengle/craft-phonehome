{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "version": "1.4.0",
  "title": "Phone Home API Response Schema",
  "description": "Schema for the Phone Home API response (output of Report::getInfo() method)",
  "definitions": {
    "npmPackage": {
      "type": "object",
      "required": ["constraint", "version"],
      "properties": {
        "constraint": {
          "type": ["string", "null"],
          "description": "The declared version constraint from package.json, with any user:password@ credentials stripped from URL constraints, or null if the value could not be processed"
        },
        "version": {
          "type": ["string", "null"],
          "description": "The version resolved from package-lock.json, with any user:password@ credentials stripped, or null when the lockfile is absent, is not parsed, or does not list the package"
        }
      }
    }
  },
  "type": "object",
  "required": [
    "api_version",
    "timestamp",
    "php_version",
    "craft_version",
    "craft_edition",
    "ip_address",
    "environment",
    "dev_mode",
    "composer_lock_updated",
    "npm",
    "system",
    "plugins",
    "modules",
    "updates",
    "meta",
    "status_checks"
  ],
  "properties": {
    "build_id": {
      "type": "string",
      "description": "The unique build ID for currently deployed code"
    },
    "api_version": {
      "type": "string",
      "description": "The version of the Phone Home API"
    },
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "The timestamp when the report was generated"
    },
    "php_version": {
      "type": "string",
      "description": "The PHP version"
    },
    "craft_version": {
      "type": "string",
      "description": "The Craft CMS version"
    },
    "craft_edition": {
      "type": "string",
      "description": "The Craft CMS edition"
    },
    "ip_address": {
      "type": "string",
      "description": "The IP address of the server"
    },
    "environment": {
      "type": "string",
      "description": "The environment name"
    },
    "dev_mode": {
      "type": "boolean",
      "description": "Whether dev mode is enabled"
    },
    "composer_lock_updated": {
      "type": ["string", "null"],
      "format": "date-time",
      "description": "The timestamp when the composer.lock file was last updated, or null if it cannot be read"
    },
    "npm": {
      "type": "object",
      "description": "Declared npm packages and their resolved versions. Always an object, so that a site with no npm dependencies can be told apart from a site whose dependencies could not be read; see status",
      "required": ["status", "package_manager", "manifest_path", "lock_updated", "dependencies", "dev_dependencies"],
      "properties": {
        "status": {
          "type": "string",
          "enum": [
            "ok",
            "no_manifest",
            "unreadable_manifest",
            "no_lockfile",
            "unsupported_lockfile",
            "unreadable_lockfile"
          ],
          "description": "The outcome of collecting this section. 'ok' means package.json was read and package-lock.json was read and parsed. 'no_manifest' means no package.json was found at any searched location, so the site genuinely has no npm dependencies. 'unreadable_manifest' means the manifest could not be read or parsed, either because a package.json is present but unreadable or because the project root could not be resolved. 'no_lockfile' means the manifest was read but no lockfile of any kind was found. 'unsupported_lockfile' means the manifest was read and a yarn.lock or pnpm-lock.yaml was found, which is named but not parsed. 'unreadable_lockfile' means the manifest was read but the lockfile could not be read or parsed"
        },
        "package_manager": {
          "type": ["string", "null"],
          "enum": ["npm", "yarn", "pnpm", null],
          "description": "The package manager detected by lockfile presence. This is null when no lockfile was found, and also when detection could not be completed, which a status of unreadable_lockfile indicates. Only npm lockfiles are parsed; yarn and pnpm are detected and named, but their declared packages report a null version"
        },
        "manifest_path": {
          "type": ["string", "null"],
          "description": "Where the package.json that was read lives, expressed relative to Craft's @root, for example '.' or '..'. Null when no manifest was found. Always relative, so no absolute server path is disclosed. This makes a misplaced manifest diagnosable from the report itself"
        },
        "lock_updated": {
          "type": ["string", "null"],
          "format": "date-time",
          "description": "The timestamp when the detected lockfile was last updated. This is null when no lockfile was found, and also when detection could not be completed. This is reported for yarn and pnpm lockfiles too, even though they are not parsed"
        },
        "dependencies": {
          "type": "object",
          "description": "Declared production dependencies keyed by package name",
          "additionalProperties": {
            "$ref": "#/definitions/npmPackage"
          }
        },
        "dev_dependencies": {
          "type": "object",
          "description": "Declared development dependencies keyed by package name",
          "additionalProperties": {
            "$ref": "#/definitions/npmPackage"
          }
        }
      }
    },
    "system": {
      "type": "object",
      "description": "System information",
      "properties": {
        "php": {
          "type": "object",
          "properties": {
            "name": {
              "type": "string",
              "description": "The name of the PHP interpreter"
            },
            "version": {
              "type": "string",
              "description": "The PHP version"
            },
            "info": {
              "type": ["object", "null"],
              "description": "Detailed PHP information (only included if expandPhpInfo is true)"
            }
          }
        },
        "os": {
          "type": "object",
          "properties": {
            "name": {
              "type": "string",
              "description": "The operating system name"
            },
            "version": {
              "type": "string",
              "description": "The operating system version"
            }
          }
        },
        "database": {
          "type": "object",
          "properties": {
            "name": {
              "type": "string",
              "description": "The database driver name"
            },
            "version": {
              "type": "string",
              "description": "The database server version"
            }
          }
        },
        "image": {
          "type": "object",
          "properties": {
            "name": {
              "type": "string",
              "description": "The image driver name (GD or Imagick)"
            },
            "version": {
              "type": "string",
              "description": "The image driver version"
            }
          }
        },
        "craft": {
          "type": "object",
          "properties": {
            "version": {
              "type": "string",
              "description": "The Craft CMS version"
            },
            "edition": {
              "type": "string",
              "description": "The Craft CMS edition"
            },
            "update_status": {
              "type": "object",
              "properties": {
                "total_available_updates": {
                  "type": "integer",
                  "description": "The total number of available updates"
                },
                "pending_migrations": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  },
                  "description": "The list of pending migration handles"
                },
                "was_craft_breakpoint_skipped": {
                  "type": "boolean",
                  "description": "Whether a Craft breakpoint was skipped"
                },
                "is_update_pending": {
                  "type": "boolean",
                  "description": "Whether an update is pending"
                },
                "is_craft_update_pending": {
                  "type": "boolean",
                  "description": "Whether a Craft update is pending"
                },
                "is_plugin_update_pending": {
                  "type": "boolean",
                  "description": "Whether a plugin update is pending"
                },
                "is_critical_update_available": {
                  "type": "boolean",
                  "description": "Whether a critical update is available"
                }
              }
            },
            "requirements": {
              "type": "array",
              "items": {
                "type": "object"
              },
              "description": "The Craft CMS requirements status"
            },
            "aliases": {
              "type": "object",
              "additionalProperties": {
                "type": "string"
              },
              "description": "The Craft CMS aliases"
            }
          }
        }
      },
      "additionalProperties": {
        "type": "string",
        "description": "Additional system information (Yii, Twig, Guzzle versions if available)"
      }
    },
    "plugins": {
      "type": "object",
      "description": "Information about installed plugins",
      "additionalProperties": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "The plugin name"
          },
          "handle": {
            "type": "string",
            "description": "The plugin handle"
          },
          "description": {
            "type": "string",
            "description": "The plugin description"
          },
          "version": {
            "type": "string",
            "description": "The plugin version"
          },
          "is_installed": {
            "type": "boolean",
            "description": "Whether the plugin is installed"
          },
          "is_enabled": {
            "type": "boolean",
            "description": "Whether the plugin is enabled"
          },
          "is_upgrade_available": {
            "type": "boolean",
            "description": "Whether an upgrade is available for the plugin"
          }
        }
      }
    },
    "modules": {
      "type": "object",
      "description": "Information about installed modules",
      "additionalProperties": {
        "type": "object",
        "properties": {
          "class": {
            "type": "string",
            "description": "The module class name"
          }
        }
      }
    },
    "updates": {
      "type": "array",
      "description": "Information about available updates",
      "items": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "The name of the package with an available update"
          },
          "abandoned": {
            "type": ["boolean", "null"],
            "description": "Whether the package is abandoned"
          },
          "status": {
            "type": ["string", "null"],
            "description": "The status of the package"
          },
          "version": {
            "type": "string",
            "description": "The version of the update"
          },
          "package": {
            "type": "string",
            "description": "The package name"
          },
          "critical": {
            "type": "boolean",
            "description": "Whether the update is critical"
          },
          "release_date": {
            "type": "string",
            "format": "date-time",
            "description": "The release date of the update"
          }
        }
      }
    },
    "meta": {
      "type": ["object", "array"],
      "description": "Additional metadata from environment variables",
      "additionalProperties": {
        "type": "string"
      }
    },
    "status_checks": {
      "type": "array",
      "description": "Results from registered status checks for monitoring application health",
      "items": {
        "type": "object",
        "required": ["name", "status"],
        "properties": {
          "name": {
            "type": "string",
            "description": "The human-readable name of the status check"
          },
          "status": {
            "type": "string",
            "enum": ["ok", "warning", "critical"],
            "description": "The status level returned by the check"
          },
          "description": {
            "type": ["string", "null"],
            "description": "Optional description of what the check monitors"
          },
          "meta": {
            "type": "object",
            "description": "Optional additional diagnostic information from the check",
            "additionalProperties": true
          }
        }
      }
    }
  }
}
